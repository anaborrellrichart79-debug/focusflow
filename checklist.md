# ✅ Checklist de FocusFlow

Este fichero recoge en todo momento qué está hecho y qué queda pendiente en el desarrollo de FocusFlow. Se actualiza en cada sesión de trabajo.

## 👉 Empezar aquí la próxima sesión

Las Fases 1, 2, 3 y 4 están completas. Toda la ampliación de funcionalidades propuesta (bloques 1 a 4: **Fase 5 — Planificación temporal**, **Bloque 2 — Estructura de las tareas**, **Bloque 3 — Revisión y hábitos**, **Bloque 4 — Analítica más profunda**) está completa, y además se ha añadido **Sincronización con Google Calendar** (ver su sección más abajo). Batería de tests: **backend 76 tests unitarios / frontend 154 tests** (el recuento del backend es anterior al Modo Escolar y no se ha vuelto a contar en esta sesión). **Modo Escolar**: hechas las fases A, B, C y D; siguiente paso, la **Fase E** (página "Planificador escolar").

Solo queda fuera, deliberadamente, la colaboración multiusuario (compartir objetivos, comentarios) por ser un salto de complejidad mucho mayor (rediseño del modelo de permisos/roles) — retomar solo si se pide explícitamente. La app está en un estado sólido y funcional de principio a fin.

**Aviso para la próxima sesión sobre Google Calendar**: el código de la sincronización está completo y probado (con Prisma/OAuth mockeados), pero para probarla de verdad de principio a fin hace falta que el usuario cree sus propias credenciales OAuth 2.0 en Google Cloud Console (nadie más puede hacerlo por él) y las rellene en `backend/.env` (`GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`). Instrucciones completas en la sección de abajo.

**Aviso importante para la próxima sesión**: si tras cambiar `backend/prisma/schema.prisma` y regenerar el cliente (`pnpm prisma generate`) el backend en modo watch (`pnpm start:dev`) se queda pillado mostrando un error de compilación TypeScript que no tiene sentido (p. ej. "no exported member" en un tipo que sí existe), **no asumas que es un falso positivo transitorio**: párate y compilealo de verdad. `pnpm test` (vitest) y `pnpm lint` (oxlint) en el backend **no hacen type-check real** — vitest usa esbuild y descarta los tipos, así que un `import type` roto no revienta los tests, y oxlint en este caso tampoco lo detectó. La única forma fiable de comprobar que el backend compila de verdad es `npx tsc --noEmit -p tsconfig.json` (no hay script `pnpm` para esto todavía). Ocurrió justo esto en esta sesión: un `import type { Tarea } from '../generated/prisma/models.js'` quedó roto (Prisma 7 con el generador `prisma-client` nombra el tipo del modelo base `TareaModel`, no `Tarea`, para no chocar con `TareaWhereInput`/`TareaCreateInput`/etc.) y el servidor siguió sirviendo la versión compilada anterior (sin la lógica de recurrencia) durante buena parte de la sesión sin ningún error visible en la app ni en los tests.

Antes de continuar, recuerda levantar Docker (`docker compose up -d` en la raíz del proyecto) — Postgres no arranca solo.

## Documentación

- [x] README.md inicial con visión, características, stack tecnológico, estructura de carpetas y roadmap.
- [x] checklist.md de seguimiento (este fichero).
- [x] comandos.md con explicación de los comandos usados (no se sube a GitHub).

## Tests automatizados

- [x] Backend: `vitest` ya venía configurado en el scaffold de NestJS (`pnpm test`, `pnpm test:watch`, `pnpm test:cov`) pero no había ningún test real, solo el de ejemplo (`aplicacion.controller.spec.ts`). Se añadieron tests unitarios para los tres servicios con lógica de negocio, mockeando `ServicioPrisma` por completo (no necesitan Postgres/Docker):
  - `autenticacion.service.spec.ts`: rechaza registro con correo duplicado, la contraseña guardada nunca es la de texto plano (se comprueba con `bcrypt.compare`), login rechaza correo inexistente y contraseña incorrecta, `obtenerUsuarioPorId` nunca devuelve el hash.
  - `objetivos.service.spec.ts`: cálculo de `totalTareas`/`tareasCompletadas` al listar, y que `actualizar`/`eliminar` **no llegan a tocar la base de datos** si el objetivo pertenece a otro usuario (aislamiento por usuario).
  - `tareas.service.spec.ts`: mismo aislamiento por usuario para tareas, y que no se puede crear/mover una tarea a un objetivo que no es del usuario.
  - `autenticacion.controller.spec.ts`, `objetivos.controller.spec.ts`, `tareas.controller.spec.ts`: comprueban que cada controlador delega en su servicio pasando el `usuario.id` que llega del decorador `@UsuarioActual()` (el de la petición autenticada), nunca uno que pudiera venir en el body o en la query.
  - Se corrigió además `backend/test/app.e2e-spec.ts`: era el test e2e de ejemplo del scaffold de Nest, sin adaptar nunca al proyecto real (importaba un `AppModule` inexistente desde `src/app.module.js` y un tipo `supertest/types` que no existe en la versión instalada de `supertest`). Ahora importa `AplicacionModule` y comprueba el mensaje real de la API. **Ejecutado con éxito** (`pnpm test:e2e`, con Docker/Postgres levantados): arranca la aplicación Nest completa de verdad (con conexión real a la base de datos) y comprueba la respuesta de `GET /`.
- [x] Frontend: no había ninguna herramienta de test instalada. Se añadió `vitest` + `@testing-library/react` (config en `vite.config.ts`, fichero de setup en `src/pruebas/configuracion.ts`, scripts `pnpm test`/`pnpm test:watch`):
  - `pomodoroSlice.test.ts`: lógica pura del temporizador (tick, cambio de fase trabajo↔descanso, y el caso más delicado — cada 4º ciclo pasa a descanso *largo* en vez de corto).
  - `interfazSlice.test.ts`: cambio de idioma, alternar tema claro/oscuro y su persistencia en `localStorage`.
  - `tareasSlice.test.ts` / `objetivosSlice.test.ts`: los `extraReducers` de los thunks (cargar/crear/eliminar/cambiar estado) probados sin red real, despachando directamente las acciones `.fulfilled`/`.rejected` que exponen los thunks de Redux Toolkit.
  - `sesionSlice.test.ts`: login/registro guardan el token en `localStorage`, `cerrarSesion` y un `restaurarSesion.rejected` (token caducado) lo borran, y un caso con `vi.resetModules()` + import dinámico para comprobar que si ya había un token guardado *antes* de arrancar la app, el estado inicial empieza en modo "restaurando sesión".
  - `SelectorTema.test.tsx`: test de componente con Redux Toolkit + react-intl reales; simula el clic del usuario y comprueba que cambia el estado y el `aria-pressed`.
  - Al escribir estos tests se detectó y arregló un bug real en `interfazSlice.ts`: si `window.matchMedia` no existe (algunos entornos), el código original (`window.matchMedia?.(...).matches`) lanzaba un `TypeError` porque el `?.` solo protegía la llamada, no el `.matches` posterior.
- [x] Backend, ronda 2: `jwt.strategy.spec.ts` (`EstrategiaJwt` lee `JWT_SECRET` del `ConfigService` al construirse — falla rápido si falta — y `validate()` mapea `sub`→`id` sin filtrar más campos de la carga útil). Se extrajo la fábrica del decorador `@UsuarioActual()` a una función exportada aparte (`obtenerUsuarioActual` en `usuario-actual.decorator.ts`) siguiendo el patrón que documenta NestJS para poder probarla sin un `ExecutionContext` real (`usuario-actual.decorator.spec.ts`): comprueba que devuelve exactamente lo que `EstrategiaJwt` dejó en `request.user`. No se tocaron `AuthGuard('jwt')` ni `PassportStrategy` en sí (son de Passport/Nest, no código propio que probar).
- [x] Frontend, ronda 2: se creó un helper compartido `src/pruebas/render.tsx` (`renderizarPagina`) que monta cualquier página con una tienda Redux nueva + `IntlProvider` + `MemoryRouter`, aceptando un `estadoPrecargado` parcial. Truco usado en casi todos los tests de páginas: **sin token de sesión**, los thunks que se despachan al montar (`cargarTareas`, `cargarObjetivos`, `cargarHistorialPomodoro`...) se rechazan al instante con "No autenticado" sin tocar la red, así que se puede precargar directamente el estado que se quiere comprobar sin mockear `fetch` — solo hace falta mockear `fetch` de verdad en los tests que comprueban una acción de escritura (login, registro, crear un objetivo). Con esto, **las 8 páginas ya tienen test**: `PaginaInicio`, `PaginaLogin`, `PaginaRegistro`, `PaginaObjetivos`, `PaginaKanban`, `PaginaEisenhower`, `PaginaPomodoro`, `PaginaEstadisticas` (esta última valida de verdad, a través del componente real, el cálculo de pomodoros "hoy"/"esta semana"/minutos escrito en la ronda anterior).

## Fase 1 — MVP

- [x] Scaffolding del monorepo (`/frontend` y `/backend`).
- [x] Backend: proyecto NestJS + TypeScript inicial (renombrado a convención en castellano: `AplicacionModule`, `AplicacionController`, `AplicacionService`).
- [x] Backend: configuración de Prisma + PostgreSQL (schema inicial con modelo `Usuario`, `ServicioPrisma`/`ModuloPrisma` conectados al `AplicacionModule`).
- [x] Backend: `docker-compose.yml` con PostgreSQL para desarrollo local (Docker Desktop instalado, contenedor `focusflow-postgres` levantado y probado).
- [x] Backend: primera migración de Prisma aplicada (tabla `usuarios` creada en la base de datos real).
- [x] Backend: conexión de Prisma a PostgreSQL en tiempo de ejecución probada (`pnpm start:dev` arranca y conecta sin errores).
- [x] Backend: registro de usuario (`POST /autenticacion/registro`, email + contraseña segura validada, hash con bcrypt).
- [x] Backend: login de usuario (`POST /autenticacion/login`, JWT firmado con `@nestjs/jwt`).
- [x] Backend: ruta protegida de ejemplo `GET /autenticacion/perfil` (guard JWT con Passport, `EstrategiaJwt`).
- [x] Backend: CRUD de objetivos (`/objetivos`) y tareas (`/tareas`), con progreso (`totalTareas`/`tareasCompletadas`) calculado en el listado, aislamiento por usuario y borrado en cascada objetivo → tareas.
- [x] Frontend: proyecto React + TypeScript (Vite) inicial.
- [x] Frontend: Tailwind CSS v4 + shadcn/ui configurados (preset "nova", base Radix, alias `@/*`).
- [x] Frontend: Redux Toolkit configurado (store base en `src/almacen/`, slice `interfazSlice` para el idioma).
- [x] Frontend: selector de idioma funcional (react-intl) probado en navegador con los 6 idiomas.
- [x] Frontend: formulario de registro/login conectado al backend (`PaginaRegistro`, `PaginaLogin`, slice `sesionSlice` con token persistido en `localStorage` y restauración de sesión al recargar).
- [x] Frontend: gestión de tareas/objetivos desde la UI (`PaginaObjetivos`, ruta `/objetivos` protegida con `RutaProtegida`): crear objetivos, añadir tareas dentro de un objetivo o sueltas, marcar como completadas con checkbox, barra de progreso, eliminar objetivo/tarea.
- [x] Frontend: temporizador Pomodoro funcional (`PaginaPomodoro`, ruta `/pomodoro` protegida): trabajo 25 min → descanso corto 5 min, descanso largo de 20 min cada 4 ciclos, botones Iniciar/Pausar/Reiniciar, aviso sonoro (Web Audio, sin ficheros externos) al cambiar de fase.

## Fase 2 — Organización

- [x] Tablero Kanban (`PaginaKanban`, ruta `/kanban` protegida): 3 columnas (Por hacer / En proceso / Hecha), tareas movibles con flechas ←/→ y con arrastrar y soltar (drag-and-drop nativo), etiqueta con el objetivo al que pertenece cada tarea, captura rápida de tareas nuevas directamente desde el tablero.
- [x] Matriz de Eisenhower (`PaginaEisenhower`, ruta `/eisenhower` protegida): 4 cuadrantes (Hacer ya / Planificar / Delegar / Eliminar) según los campos `Tarea.urgente` y `Tarea.importante`; cada tarjeta tiene botones para alternar ambos criterios y moverla de cuadrante al instante.
- [x] Captura rápida estilo GTD (`CapturaRapida`): barra flotante fija visible en todas las páginas protegidas (Objetivos, Kanban, Eisenhower, Pomodoro), con un único campo de texto para anotar cualquier tarea al vuelo sin objetivo asignado; luego se organiza (Kanban/Eisenhower/objetivo) desde cualquiera de las otras vistas.

## Fase 3 — Analítica

- [x] Estadísticas de progreso visual (`PaginaEstadisticas`, ruta `/estadisticas` protegida): % global completado, distribución de tareas por estado (gráfico de barra apilada con leyenda, paleta validada para daltonismo), progreso por objetivo (barras reutilizadas de Objetivos).
- [x] Detección/marcado de tareas de alto impacto (Pareto 80/20): campo `Tarea.esAltoImpacto`, marcado con ★ desde el tablero Kanban, visualizado en Estadísticas con recuento, porcentaje sobre el total y listado de las tareas marcadas.

## Fase 4 — Pulido

- [x] Selector de idioma funcional (react-intl) con los 6 idiomas: castellano, valenciano, gallego, vasco, catalán, inglés (mensajes de ejemplo únicamente, ver pendiente de traducciones completas).
- [x] Traducciones completas para los 6 idiomas de todo lo construido hasta ahora (auth, objetivos/tareas, Pomodoro, Kanban, Eisenhower, captura rápida, estadísticas/Pareto).
- [x] Locale de `Intl.NumberFormat`/`DateTimeFormat` para eu/gl/va/ca: la causa real del aviso en consola era que `"va"` (valenciano) no es una etiqueta BCP-47/ICU válida (`Intl.NumberFormat.supportedLocalesOf('va')` devuelve `[]`; comprobado con Node). Se añadió `CODIGO_LOCALE_ICU` en `frontend/src/idiomas/index.ts`, que traduce `"va"` a la etiqueta correcta `"ca-ES-valencia"` (catalán, variante valenciana) solo para lo que se le pasa a `Intl`/`IntlProvider`; el resto del código sigue usando `"va"` como código interno. eu/gl/ca/es/en ya eran válidos de por sí.
- [x] Accesibilidad (a11y): `document.documentElement.lang` se sincroniza con el idioma elegido; se restauró el anillo de foco del campo de `CapturaRapida` (tenía `focus-visible:ring-0`, quedaba sin ningún indicador visible al navegar con teclado); botones de alternar (Eisenhower urgente/importante, estrella de alto impacto en Kanban) llevan `aria-pressed` para anunciar su estado a lectores de pantalla; el checkbox de completar tarea en `ObjetivoTarjeta` tiene `aria-label` con el título de la tarea. El resto (botones de shadcn/ui, checkboxes de Radix) ya traía foco visible y `aria-label` de fábrica.
- [x] Modo oscuro: `interfazSlice` guarda `tema` (`'claro' | 'oscuro'`), con valor inicial desde `localStorage` o `prefers-color-scheme` del sistema si no hay nada guardado. Componente `SelectorTema` (botón 🌙/☀️ junto al `SelectorIdioma` en `PaginaInicio`) que alterna la clase `.dark` en `<html>` y el `<meta name="theme-color">`.
- [x] PWA: `public/manifest.webmanifest` (nombre, iconos, `display: standalone`) enlazado desde `index.html`; `public/sw.js` con estrategia stale-while-revalidate para peticiones `GET` al propio origen (nunca a la API, que es otro origen); registrado desde `main.tsx` solo en producción. `index.html` también actualizado: `<title>FocusFlow</title>` (antes "frontend"), meta descripción, `lang="es"`.
- [x] Iconos PNG dedicados de la PWA (`frontend/public/icons/icono-192.png` y `icono-512.png`, fondo blanco sólido, referenciados en el manifest junto al `favicon.svg` original) generados a partir del logo SVG existente. `sharp`/librsvg (usado para rasterizar SVG server-side) no soportaba bien los filtros de desenfoque del SVG y generaba barras negras; se resolvió renderizando el SVG en Chrome real (motor de renderizado completo, sí soporta esos filtros) sobre una página HTML servida por un servidor local temporal, capturando la región con la herramienta de zoom del navegador, y solo usando `sharp` al final para el redimensionado limpio a 192x192/512x512 sobre un PNG ya rasterizado (sin más filtros SVG de por medio). `apple-touch-icon` en `index.html` actualizado para usar el PNG de 192 (iOS no soporta bien SVG ahí).
- [x] Paleta de color moderna y motivadora (`frontend/src/index.css`): se sustituyó el tema gris neutro por defecto del preset "nova" por índigo (`--primary`, `#4f46e5` claro / `#818cf8` oscuro — foco y confianza, el color más asociado a productividad) como color principal de interacción (botones, barra de progreso, checkbox activo, anillo de foco), y dos tokens nuevos registrados en `@theme inline` para uso semántico en toda la app: `--exito` (verde, tareas completadas) y `--motivador` (ámbar, energía/alto impacto). El color de fondo al pasar el ratón (`--accent`) ya no es gris fijo: se deriva del primario con `color-mix(in oklch, var(--primary) 10%, var(--background))`, así se adapta solo a claro/oscuro sin duplicar valores. Se reutilizaron los colores ya validados para daltonismo (`--chart-1/2/3`, azul/naranja/verde) para colorear también los encabezados del tablero Kanban (mismo código de color que ya usaba el gráfico de Estadísticas, no un color nuevo) y se añadió un cuarto color por cuadrante en la Matriz de Eisenhower (rojo urgente+importante / azul planificar / ámbar delegar / gris eliminar); en ambos casos el color es un refuerzo visual, nunca el único indicador, porque el texto de la columna/cuadrante y las etiquetas urgente/importante ya transmiten la misma información. `Card` ganó una sombra suave con hover (`shadow-sm hover:shadow-md`) y `--radius` subió de `0.625rem` a `0.75rem` para un aspecto general más suave. Además, todas las piezas clicables (botones de `Button` salvo `ghost`/`link`, los `<select>` nativos, y las tarjetas de tarea de Kanban/Eisenhower) llevan ahora `shadow-sm` con `hover:shadow-md` para que se perciban como interactivas.
- [x] Estilos responsive para los 4 tamaños de pantalla (móvil pequeño, móvil normal, tablet, pantalla grande): auditado navegando la app con el viewport forzado a 320/400/480/640/768/1440px (vía un iframe con `transform: scale()` embebido en una pestaña de Chrome, porque en este entorno Windows `resize_window` no cambia el viewport real de la extensión). Se encontró y arregló un problema real: la cuadrícula de estadísticas de Pomodoro (`PaginaEstadisticas`) usaba `grid-cols-3` fijo sin ningún breakpoint, así que en móvil las etiquetas largas ("Minutos de trabajo esta semana") se apretujaban en columnas de ~75px. Se añadió un breakpoint nuevo `--breakpoint-xs: 26.25rem` (420px, registrado en `@theme inline`) para distinguir móvil pequeño de móvil normal: por debajo de 420px la cuadrícula apila en 1 columna a ancho completo, y desde 420px (móvil normal en adelante) vuelve a 3 columnas. Las cabeceras de página (Kanban/Eisenhower/Objetivos/Estadísticas: título + botón "FocusFlow") ganaron `flex-wrap` por seguridad ante textos largos en otros idiomas en pantallas muy estrechas. El resto de la app (grid de Kanban a `sm:grid-cols-3`, matriz de Eisenhower a `sm:grid-cols-2`, formularios de login/registro/objetivos con `max-w-sm`/`max-w-2xl`, temporizador Pomodoro, botones de `PaginaInicio` con `flex-wrap`) ya funcionaba bien en todos los tamaños probados; solo se le dio un poco más de aire en pantalla grande (`lg:gap-6` en Kanban/Eisenhower, `lg:text-6xl` en el título de inicio).

## Historial de Pomodoro

- [x] Backend: nuevo modelo `SesionPomodoro` en `schema.prisma` (enum `FasePomodoro`, `duracionSegundos`, `usuarioId`, `tareaId` opcional con `onDelete: SetNull` — a diferencia de Objetivo→Tarea, que es en cascada, aquí se conserva el historial aunque se borre la tarea). Migración `20260918164219_historial_pomodoro` generada y aplicada contra Postgres real. Módulo `pomodoro/` (`ModuloPomodoro`) con `POST /pomodoro/sesiones` (registra una fase completada, comprobando que la tarea asociada — si la hay — es del usuario) y `GET /pomodoro/sesiones` (últimas 100 — el límite se subió de 20 a 100 para que las estadísticas de "hoy"/"esta semana" tengan margen suficiente sin necesitar un endpoint de agregación propio —, con el título de la tarea incluido). Probado con tests unitarios y con `curl` real contra la base de datos, incluido el caso de intentar asociar la sesión a la tarea de otro usuario (rechazado con 404).
- [x] Frontend: `pomodoroSlice` guarda qué fase se acaba de completar (`ultimaFaseCompletada`) para poder registrarla en el momento justo — antes, en cuanto sonaba el aviso, `estado.fase` ya apuntaba a la *siguiente* fase, no a la que acababa de terminar. `PaginaPomodoro` añade un desplegable "Tarea asociada (opcional)" (con las tareas no completadas del usuario) y una tarjeta "Historial reciente" (solo las 5 últimas, aunque el store guarda hasta 100 para las estadísticas) que carga las últimas sesiones al entrar en la página. Probado en Chrome con una cuenta real: se crea una tarea en Objetivos, aparece en el desplegable de Pomodoro, y una sesión registrada por la API aparece en el historial nada más recargar la página.
- [x] Estadísticas de Pomodoro en `PaginaEstadisticas`: tarjeta con pomodoros de trabajo completados hoy, completados esta semana y minutos de trabajo esta semana, calculados en el frontend a partir del mismo historial (igual que el resto de Estadísticas, sin endpoint de agregación dedicado). Probado en Chrome: tras registrar una sesión de 25 min, la tarjeta mostró "1 / 1 / 25" correctamente.
- [x] Tests: `pomodoro.service.spec.ts` y `pomodoro.controller.spec.ts` en el backend (mismo patrón de aislamiento por usuario que Objetivos/Tareas); `pomodoroSlice.test.ts` ampliado con los nuevos `extraReducers` del historial.

## Fase 5 — Planificación temporal

- [x] Backend: campo `fechaLimite DateTime?` opcional añadido a `Tarea` y `Objetivo` en `schema.prisma` (migración `20260921172009_fecha_limite`). `CrearTareaDto`/`CrearObjetivoDto` lo aceptan con `@Type(() => Date)` + `@IsDate()` de `class-transformer`/`class-validator` (no `@IsISO8601()` sobre un `string`): Prisma 7 en tiempo de ejecución exige un `DateTime` ISO-8601 completo y rechaza con `PrismaClientValidationError: premature end of input` una fecha "solo día" como la que manda un `<input type="date">` del navegador (p. ej. `"2026-09-22"`); convertirla a un objeto `Date` en el DTO antes de que llegue a Prisma lo soluciona (detectado y arreglado tras probarlo de verdad en Chrome, no solo con tests unitarios con Prisma mockeado, que no habrían pillado este caso). `TareasService.crear`/`ObjetivosService.crear` pasan el campo igual que `titulo`/`descripcion`; `actualizar` ya lo cubre al pasar el DTO completo.
- [x] Frontend: nueva utilidad `frontend/src/utilidades/fechas.ts` (`diasHastaFecha`, `esUrgentePorFecha`, `formatearFechaRelativa` con `Intl.RelativeTimeFormat` — así "mañana"/"dentro de 3 días"/"hace 2 días" sale traducido automáticamente al idioma activo sin tener que mantener esas frases a mano en los 6 ficheros de idioma) y componente `EtiquetaFechaLimite` (badge con 📅 + fecha relativa, en rojo si está vencida, en ámbar `--motivador` si vence en 2 días o menos). Añadido a los formularios de creación de tarea/objetivo (Objetivos, `ObjetivoTarjeta`, Kanban) y mostrado en las tarjetas de Kanban, Eisenhower, `ObjetivoTarjeta` y la lista de tareas sueltas.
- [x] Widget "Próximos 7 días" en `PaginaInicio` (solo con sesión iniciada): lista las tareas no completadas con `fechaLimite` dentro de los próximos 7 días, ordenadas por cercanía; reutiliza `tareasSlice` (se añade `cargarTareas()` al montar la página, patrón ya usado en el resto de páginas) en vez de un endpoint propio.
- [x] La Matriz de Eisenhower "se alimenta" de la fecha límite tal como se pidió: una tarea se considera urgente para decidir el cuadrante si `tarea.urgente` es `true` **o** si `esUrgentePorFecha(tarea.fechaLimite)` (vence en ≤2 días o ya venció), sin mutar el campo `urgente` guardado — es un cálculo de "urgencia efectiva" solo para la clasificación visual. Cuando una tarea cae en un cuadrante por la fecha y no por el botón "Urgente" manual, se muestra una etiqueta "⏰ Urgente por fecha" para que quede claro por qué está ahí. El botón "Urgente" sigue reflejando y controlando únicamente el valor real guardado.
- [x] Tests: `utilidades/fechas.test.ts` (con `vi.useFakeTimers()`, todas las fechas de prueba fijadas a mediodía UTC para que el cálculo de "día de calendario" no dependa de la zona horaria de quien ejecute los tests); `PaginaEisenhower.test.tsx` ampliado con el caso de clasificación automática por fecha; `PaginaInicio.test.tsx` ampliado con el widget "Próximos 7 días"; `tareas.service.spec.ts`/`objetivos.service.spec.ts` ampliados con el paso de `fechaLimite` a Prisma.
- [ ] Pendiente si se retoma: vista de calendario/agenda (día/semana) y recordatorios por Web Push (requiere backend con claves VAPID) — quedaron fuera del alcance de esta fase por ser un desarrollo bastante más grande cada uno.

## Bloque 2 — Estructura de las tareas

- [x] Backend: dos modelos nuevos en `schema.prisma` (migraciones `20260921174314_subtareas_y_etiquetas` y `20260921174758_recurrencia`):
  - `Subtarea` (`titulo`, `completada`, `tareaId` con `onDelete: Cascade`): checklist dentro de una tarea, para tareas más grandes que un Pomodoro sin llegar a crear un objetivo nuevo. Módulo propio `subtareas/` con `SubtareasController` sin prefijo de ruta fijo (`@Controller()`): `POST /tareas/:tareaId/subtareas` (comprueba que la tarea padre es del usuario), `PATCH /subtareas/:id` y `DELETE /subtareas/:id` (ambas comprueban propiedad vía join `tarea: { usuarioId }`, no hay `usuarioId` directo en `Subtarea`).
  - `Etiqueta` (`nombre` único por usuario vía `@@unique([usuarioId, nombre])`, relación muchos-a-muchos implícita `Tarea.etiquetas` ↔ `Etiqueta.tareas`): etiquetas libres para filtrar por área de vida o proyecto. Módulo `etiquetas/` con `GET /etiquetas` (listado del usuario, para autocompletar). Se crean/reutilizan solas al guardar una tarea: `CrearTareaDto`/`ActualizarTareaDto` aceptan `etiquetas?: string[]` (nombres), y `TareasService` los traduce a escritura Prisma con `connectOrCreate` (en `crear`) o `set: [] + connectOrCreate` (en `actualizar`, para reemplazar el conjunto completo, no solo añadir); los nombres se limpian de espacios y duplicados antes.
  - `Tarea.recurrencia` (enum `NINGUNA | DIARIA | SEMANAL`, por defecto `NINGUNA`): al marcar como `HECHA` (en `TareasService.actualizar`) una tarea que tenía `recurrencia` distinta de `NINGUNA` y no estaba ya `HECHA` (para no duplicar si se reenvía el mismo PATCH), se crea automáticamente la siguiente ocurrencia — mismo título/descripción/objetivo/urgente/importante/recurrencia, `fechaLimite` avanzada un día o siete desde la `fechaLimite` anterior (o desde hoy si no tenía), estado `POR_HACER`. La nueva ocurrencia **no** hereda etiquetas ni subtareas: son específicas de la ejecución que se acaba de completar, no de la rutina en sí.
  - `GET /tareas` (y las respuestas de `crear`/`actualizar`) incluyen ahora `subtareas` (ordenadas por `creadoEn`) y `etiquetas` en cada tarea (`include` en Prisma), para que el frontend no necesite peticiones adicionales.
  - La `descripcion` de la tarea (el campo ya existía en el modelo desde el principio, pero ningún formulario la pedía ni la mostraba) ahora se edita desde el frontend.
- [x] Frontend: componente `DetalleTarea` (`frontend/src/componentes/DetalleTarea.tsx`) — sustituye el `<span>{tarea.titulo}</span>` de cada tarjeta/fila de tarea (Kanban, Eisenhower, `ObjetivoTarjeta`, tareas sueltas de Objetivos) por un botón que abre un `Dialog` (shadcn/Radix, añadido con `pnpm exec shadcn add dialog textarea badge`) con: notas (`Textarea`, se guarda al perder el foco), recurrencia (`<select>` Ninguna/Diaria/Semanal), etiquetas (chips `Badge` con botón de quitar + campo con `<datalist>` que autocompleta desde `etiquetasSlice`) y subtareas (checklist con `Checkbox`, añadir/eliminar). Componente `IndicadoresTarea` muestra en la propia tarjeta (sin abrir el diálogo) las etiquetas como `Badge` y el progreso de subtareas ("☑ 1/3") cuando la tarea tiene alguna.
  - Nuevo slice `etiquetasSlice.ts` (lista de etiquetas del usuario, para el autocompletado) y ampliación de `tareasSlice.ts` con thunks para subtareas (`crearSubtareaTarea`, `cambiarSubtarea`, `eliminarSubtareaTarea`, que actualizan el array `subtareas` de la tarea concreta dentro de la lista) y para tarea (`cambiarDescripcionTarea`, `cambiarEtiquetasTarea`, `cambiarRecurrenciaTarea`).
  - `cambiarEstadoTarea` ahora despacha también `cargarTareas()` cuando el nuevo estado es `HECHA`: si la tarea era recurrente, el backend ha creado en silencio la siguiente ocurrencia, y así aparece sin esperar a la próxima navegación entre páginas.
- [x] Tests: `subtareas.service.spec.ts`, `subtareas.controller.spec.ts`, `etiquetas.service.spec.ts`, `etiquetas.controller.spec.ts` en el backend (mismo patrón de aislamiento por usuario que el resto); `tareas.service.spec.ts` ampliado con etiquetas (`connectOrCreate`/`set` vacío) y con 5 casos de recurrencia (diaria, semanal, sin `fechaLimite` previa, sin recurrencia no crea nada, no duplica si ya estaba `HECHA`). En el frontend, `DetalleTarea.test.tsx` (abrir el diálogo, ver notas/recurrencia/etiquetas/subtareas existentes, añadir un paso nuevo con `fetch` mockeado) y `tareasSlice.test.ts` ampliado con los reducers de subtareas.
- [x] Probado de verdad en Chrome de principio a fin: crear tarea → abrir su detalle → escribir notas, poner recurrencia diaria, añadir etiqueta "casa" y una subtarea, marcarla completada → cerrar el diálogo (la tarjeta ya muestra la etiqueta y "☑ 1/1") → mover la tarea a "Hecha" → aparece automáticamente una nueva tarea "por hacer" con la fecha límite avanzada un día, las mismas notas y recurrencia, pero sin la etiqueta ni la subtarea.
- [ ] Pendiente si se retoma: filtrar el tablero/las listas por etiqueta (de momento las etiquetas son solo informativas, no hay UI para filtrar por ellas todavía).

## Bloque 3 — Revisión y hábitos

Todo calculado en el frontend a partir de `tareasSlice`/`objetivosSlice` ya cargados, sin endpoints propios — mismo patrón que Estadísticas/Kanban/Eisenhower. Sin cambios de backend en este bloque.

- [x] Página "Revisión semanal" nueva (`frontend/src/paginas/PaginaRevision.tsx`, ruta `/revision` protegida, enlace "Revisión semanal" en `PaginaInicio`), con 4 tarjetas:
  - **Tareas vencidas** (🔥): tareas no completadas con `fechaLimite` ya pasada (`diasHastaFecha < 0`); reutiliza `EtiquetaFechaLimite`. No estaba en la propuesta original pero encajaba de forma natural con la revisión semanal y reutiliza infraestructura ya construida en el bloque 1.
  - **Completadas esta semana** (✅): tareas con `estado === 'HECHA'` cuya `actualizadoEn` cae en los últimos 7 días. Aproximación deliberada: el modelo no guarda una fecha de "completado" específica (`actualizadoEn` se actualiza con cualquier cambio), pero en la práctica el último cambio de una tarea completada casi siempre es justo el que la completó.
  - **Objetivos sin tocar**: objetivos cuya `actualizadoEn` tiene más de 7 días y que aún tienen tareas pendientes (o ninguna tarea todavía), para detectar objetivos abandonados.
  - **Tareas sueltas por organizar**: tareas sin `objetivoId` y no completadas (las acumuladas por Captura Rápida), con un botón directo a "Organizar en Objetivos". Una misma tarea puede aparecer aquí y en "vencidas" a la vez si aplica a ambos criterios; es intencional, cada tarjeta responde una pregunta distinta.
  - Cada tarjeta tiene su propio estado vacío positivo (p. ej. "No tienes tareas vencidas. ¡Bien hecho!") en vez de desaparecer sin más.
- [x] Racha (🔥 *streak*) de Pomodoros en `PaginaEstadisticas`: nueva utilidad `frontend/src/utilidades/rachas.ts` (`calcularRachaDias`) que cuenta días consecutivos con al menos una sesión de trabajo completada, terminando hoy o ayer (si hoy todavía no hay actividad no se da la racha por rota hasta que acabe el día, igual que apps de hábitos tipo Duolingo). Se muestra como "🔥 Racha: N días seguidos" en ámbar (`--motivador`) dentro de la tarjeta "Pomodoro" ya existente, o un mensaje neutro invitando a empezar si la racha es 0.
- [x] Tests: `utilidades/rachas.test.ts` (racha vacía, racha de 1 día, racha que sigue viva con hoy sin actividad pero ayer sí, hueco que corta la racha, varias sesiones el mismo día cuentan una vez) y `PaginaRevision.test.tsx` (las 4 secciones, con caso relleno y caso vacío cada una), ambos con `vi.useFakeTimers()` y fechas fijadas a mediodía UTC por el mismo motivo de independencia de zona horaria que el resto de tests de fechas de esta sesión. `PaginaEstadisticas.test.tsx` ampliado con la racha.
- [x] Probado en Chrome con datos reales: una tarea vencida (creada con fecha pasada), una tarea suelta pendiente, y una tarea completada al momento — las tres aparecen correctamente repartidas en sus tarjetas de `/revision`, incluida la tarea vencida apareciendo a la vez en "Tareas vencidas" y "Tareas sueltas por organizar". La racha de Pomodoro también se comprobó con historial real de la cuenta de pruebas ("🔥 Racha: 1 día seguido").

## Bloque 4 — Analítica más profunda

- [x] Backend: campo `tiempoEstimadoMinutos Int?` opcional añadido a `Tarea` (migración `20260921182147_tiempo_estimado`), validado con `@IsInt() @Min(1) @Max(1000)` en el DTO (`ActualizarTareaDto` lo hereda de `CrearTareaDto` vía `PartialType`, igual que el resto de campos). El "tiempo real" **no** necesita campo ni endpoint propio: ya existe `SesionPomodoro.tareaId`, así que se suma en el frontend a partir del historial de Pomodoro ya cargado (mismo patrón de "sin agregación en el backend" que el resto de Estadísticas).
- [x] Mapa de calor de actividad diaria (estilo GitHub) en `PaginaEstadisticas`: componente `frontend/src/componentes/MapaActividad.tsx` + utilidad `frontend/src/utilidades/actividad.ts` (`construirSemanasActividad`), 14 semanas (lunes a domingo) con recuento de sesiones de trabajo de Pomodoro por día. Color secuencial de un solo tono (índigo `--primary`, de `--muted` a `--primary` puro vía `color-mix`) en vez de una paleta categórica: para una escala de magnitud de un único hue no hace falta pasar el validador de daltonismo de la skill de dataviz (ese validador es para pares de tonos categóricos/divergentes; una rampa secuencial de un solo hue ya es segura por diseño al depender de luminosidad, no de diferenciar tonos). El detalle de cada día se da con el atributo `title` nativo (fecha + "N pomodoros") en vez de un tooltip a medida: decisión consciente de mantener la app sin ninguna librería/componente de tooltip propio, coherente con el resto del proyecto.
- [x] "Tiempo estimado vs. real" en `PaginaEstadisticas`: el tiempo estimado se pone y edita desde `DetalleTarea` (nuevo campo numérico "Tiempo estimado (minutos)"), que además muestra ahí mismo el tiempo real ya registrado (en rojo si se ha superado la estimación, en verde si no). `PaginaEstadisticas` añade una tarjeta con la lista de todas las tareas que tienen tiempo estimado, comparando estimado y real igual que en el detalle. Limitación conocida y ya existente desde el bloque 1: el historial de Pomodoro sólo trae las últimas 100 sesiones desde el backend, así que el "tiempo real" de una tarea muy antigua con muchísimas sesiones podría quedarse corto; no se ha tocado ese límite en este bloque.
- [x] Exportar a CSV: utilidad `frontend/src/utilidades/exportar.ts` (`construirFilaCsv` con el escapado CSV estándar de comillas/comas/saltos de línea, y `descargarCsv` que dispara la descarga con un `Blob` + `<a download>`, sin ninguna librería). El CSV incluye BOM (`U+FEFF`, generado con `String.fromCharCode` para no dejar el carácter invisible suelto en el código fuente — dejarlo tal cual disparaba el aviso `no-irregular-whitespace` del linter) para que Excel lo abra como UTF-8 sin romper acentos. Botón "Exportar CSV" en `PaginaEstadisticas` que descarga un informe con todas las tareas (título, objetivo, estado, fecha límite, tiempo estimado/real, etiquetas).
- [x] Exportar a PDF: sin librería de generación de PDF (evitada a propósito, sigue la misma filosofía que el resto del proyecto de no añadir dependencias pesadas si el navegador ya lo resuelve) — botón "Imprimir / PDF" que llama a `window.print()`, apoyado en una hoja de estilos `@media print` nueva en `index.css` que oculta los botones de acción/navegación (clase `.no-imprimir`) y fuerza fondo blanco/texto negro para que "Guardar como PDF" desde el diálogo de impresión del navegador salga legible sin depender del tema claro/oscuro activo.
- [x] Tests: `utilidades/actividad.test.ts` (número de semanas/días, la última columna es la semana actual y acaba en domingo, cuenta bien varias sesiones el mismo día, días sin actividad quedan en 0) y `utilidades/exportar.test.ts` (escapado de comas, comillas dobles y saltos de línea) con `vi.useFakeTimers()` donde hacía falta. `PaginaEstadisticas.test.tsx` ampliado con el mapa de actividad, la comparación de tiempo (vacía y con datos) y el botón "Exportar CSV" (mockeando `descargarCsv` con `vi.mock`, ya que `URL.createObjectURL` no existe en jsdom). `DetalleTarea.test.tsx`: el mock de `fetch` tuvo que distinguir por URL la petición de historial de Pomodoro (que ahora se dispara sola al abrir el diálogo, para poder calcular el tiempo real) de la de crear la subtarea — con un único mock genérico el historial se quedaba con la forma equivocada y rompía el cálculo.
- [x] Probado en Chrome de verdad (no solo en tests): puesto un tiempo estimado de 20 minutos a una tarea sin sesiones de Pomodoro todavía → apareció correctamente en Estadísticas como "Estimado: 20 min · Real: 0 min" en verde. Comprobado con JavaScript en la propia pestaña que la regla `@media print` está cargada en las hojas de estilo y que el contenedor de botones tiene la clase `.no-imprimir` (sin llegar a pulsar "Imprimir / PDF" de verdad, porque el diálogo nativo de impresión del navegador es modal y bloquearía la sesión de automatización). El botón "Exportar CSV" sí se pulsó de verdad: descargó `focusflow-tareas-2026-09-21.csv` a la carpeta de Descargas real, con el BOM y las 3 tareas de prueba correctamente escapadas; el archivo se borró después por ser un artefacto de la prueba.

## Sincronización con Google Calendar

Pedida explícitamente por el usuario, fuera de los 4 bloques de ampliación originales. Alcance acordado: preparar todo el código ahora (las credenciales OAuth las tiene que crear el propio usuario más adelante, con su propia cuenta de Google — no es algo que se pueda hacer por él), sincronización manual + automática cada pocos minutos mientras la pestaña está abierta, y sincroniza tanto tareas con fecha límite como sesiones de Pomodoro completadas.

- [x] Backend: modelo `EventoCalendarioGoogle` nuevo en `schema.prisma` (migración `20260921184402_google_calendar`) — lleva la cuenta de qué tarea o sesión de Pomodoro ya tiene su evento gemelo en Google Calendar (relación opcional y única a `Tarea` y a `SesionPomodoro`, nunca las dos a la vez), para no duplicar eventos en cada sincronización y para saber a qué tarea traducir un evento que llega importado desde el calendario. `Usuario` gana los campos `googleAccessToken`/`googleRefreshToken`/`googleTokenExpiraEn`/`googleUltimaSincronizacion`, **sin cifrar** (mismo nivel de seguridad que el resto del proyecto — `JWT_SECRET` en claro en `.env` — aceptable para un proyecto personal en desarrollo local, no para producción multiusuario real).
- [x] Backend: nuevo módulo `google/` (`ModuloGoogle`, con su propio `JwtModule.registerAsync` — no se reutiliza el de `ModuloAutenticacion`, que no lo exporta, siguiendo el patrón de módulos autocontenidos ya usado en el resto del proyecto) con `GoogleService` (usa el paquete oficial `googleapis`, elegido a propósito en vez de hablar directamente con la API REST de OAuth2/Calendar, porque ahí sí compensa una librería mantenida por Google en vez de reinventar la firma/renovación de tokens) y 5 rutas en `GoogleController`:
  - `GET /google/conectar` (protegida): genera la URL de autorización de Google. El `usuarioId` viaja firmado como JWT de 10 minutos en el parámetro `state` (patrón necesario porque `GET /google/callback` es una redirección del navegador sin cabecera `Authorization`).
  - `GET /google/callback` (**pública** a propósito): Google redirige aquí con `code` y `state`; se verifica el `state`, se intercambia el código por tokens y se guardan en el usuario correspondiente; redirige de vuelta a `FRONTEND_URL/ajustes?google=conectado` (o `...&google=error` si algo falla).
  - `POST /google/sincronizar` (protegida): ejecuta la sincronización en ambos sentidos (ver detalle abajo).
  - `DELETE /google/desconectar` (protegida): borra los tokens del usuario y todos sus `EventoCalendarioGoogle` (no los eventos de Google en sí, solo la relación local).
  - `GET /google/estado` (protegida): si hay cuenta conectada y cuándo fue la última sincronización.
  - Lógica de `sincronizar()`: (1) para cada tarea con `fechaLimite` no completada, crea o actualiza su evento en Google (evento de día completo, sin hora); si la tarea pasó a `HECHA` y tenía evento, lo borra de Google y de la relación local. (2) Empuja como evento nuevo cada sesión de Pomodoro de fase `TRABAJO` que aún no tenga evento gemelo (máximo 50 por pasada, para no saturar la API si hay mucho historial acumulado sin sincronizar nunca). (3) Importa como tarea nueva cualquier evento de Google en una ventana de los próximos 30 días que no sea ya conocido (nunca se recorre el calendario entero ni el pasado, para no volcar de golpe años de eventos ajenos a FocusFlow como si fuesen tareas). (4) Si un evento ya conocido y ligado a una tarea cambió de título o de fecha en Google, actualiza la tarea correspondiente (edición bidireccional real, no solo import de eventos nuevos). Todos los eventos que crea FocusFlow llevan `extendedProperties.private.focusflow = 'true'` para poder distinguirlos en el futuro si hiciera falta.
- [x] Tests backend: `google.service.spec.ts` (14 casos) mockeando `ServicioPrisma`, `JwtService`, `ConfigService` y el paquete `googleapis` entero con `vi.mock('googleapis', ...)` (con la trampa de que `google.auth.OAuth2` hay que mockearlo con una `function` normal, no una arrow function, porque el servicio real lo invoca con `new` y una arrow function no puede usarse como constructor). Cubre: URL de autorización (y su rechazo si no hay `GOOGLE_CLIENT_ID` configurado), validación del `state` en el callback, estado conectado/no conectado, desconexión, y en `sincronizar()`: crear evento nuevo, actualizar uno existente, borrar el de una tarea completada, empujar una sesión de Pomodoro, importar un evento externo nuevo, y no tocar nada cuando un evento ya conocido no ha cambiado.
- [x] Frontend: `servicios/google.ts` + slice `almacen/googleSlice.ts` (estado: conectado, última sincronización, sincronizando, último resumen, error) y página nueva `PaginaAjustes` (ruta `/ajustes` protegida, enlace "Ajustes" en `PaginaInicio`): muestra si la cuenta está conectada, la fecha de la última sincronización, botón "Conectar con Google" (redirige el navegador entero a la URL que da el backend) o, si ya está conectada, "Sincronizar ahora" (con resumen de creados/actualizados/eliminados/importados) y "Desconectar". Tras volver del `callback` del backend, lee `?google=conectado`/`?google=error` de la URL para mostrar un aviso de éxito o fallo.
- [x] Sincronización automática periódica: en `Aplicacion.tsx`, mientras haya sesión iniciada y la cuenta de Google esté conectada, se dispara `sincronizarGoogle()` cada 5 minutos (`setInterval`, limpiado en el `useEffect` al desmontar o si cambia el estado de conexión) además del botón manual — cumple el "manual + automático cada X minutos mientras la app está abierta" que pidió el usuario. No hay sincronización en segundo plano cuando la pestaña está cerrada (no hay webhook de Google Calendar configurado a propósito: `localhost` no tiene una URL pública HTTPS a la que Google pueda llamar).
- [x] Tests frontend: `googleSlice.test.ts` (6 casos, mismo patrón que el resto de slices — despachar directamente las acciones `.fulfilled`/`.rejected` de los thunks) y `PaginaAjustes.test.tsx` (4 casos: sin conectar, conectada, y los dos mensajes de vuelta del callback vía query param).
- [x] Probado en Chrome de verdad: la página `/ajustes` carga correctamente con el estado "No conectado", el enlace "Ajustes" aparece en `PaginaInicio` junto al resto de navegación, y al pulsar "Conectar con Google" sin credenciales configuradas en el servidor se muestra el error esperado ("La sincronización con Google Calendar no está configurada en el servidor") sin romper la UI.
- [x] **Credenciales de Google Cloud Console creadas por el usuario y flujo OAuth probado de extremo a extremo de verdad** (sesión posterior): el usuario creó el proyecto en Google Cloud Console, activó la Google Calendar API, creó las credenciales OAuth 2.0 y las rellenó en `backend/.env`. Se probó el flujo completo en Chrome con su cuenta real: "Conectar con Google" → pantalla de cuentas de Google → **paso adicional no documentado originalmente**: con la app en modo "Pruebas" en la pantalla de consentimiento OAuth, Google devuelve `access_denied` ("El desarrollador no te ha dado acceso a esta app") si la cuenta que intenta conectar no está añadida explícitamente en la sección "Usuarios de prueba" de esa pantalla — hace falta añadirla ahí además de crear las credenciales. Con la cuenta añadida como usuaria de prueba, la conexión y el "Conectar con Google" → consentimiento → vuelta a `/ajustes?google=conectado` funcionaron correctamente.

## Sincronización robusta + hora/duración de tareas + Agenda

Al probar la sincronización real de Google Calendar con la cuenta del usuario (sesión posterior a la anterior), la primera sincronización completa (184 tareas de prueba acumuladas de sesiones anteriores) tardó 1-3 minutos por ser secuencial (una llamada a la API de Google por tarea, sin batch ni paralelismo) pero terminó bien; la **segunda** sincronización devolvió `500 Internal server error` y abortó sin guardar nada. Diagnóstico: la primera pasada había importado un evento real y recurrente del calendario del usuario ("Publicar articulo", una instancia con ID de Google del tipo `..._20261012T070000Z`, propio de instancias de eventos recurrentes) como una tarea de FocusFlow; en la siguiente sincronización, el código intentaba "devolver" esa tarea a Google como evento de todo el día (`PUT` con solo `start.date`), y Google rechazaba con `400 Bad Request` el intento de convertir así una instancia de evento recurrente con hora — como el bucle no capturaba el error por tarea, uno solo tumbaba toda la sincronización.

- [x] Backend (`GoogleService.sincronizar`): cada evento individual (por tarea, por sesión de Pomodoro, por evento importado) se procesa ahora dentro de su propio `try/catch`; un fallo puntual se cuenta en un nuevo campo `errores` del resumen (`ResumenSincronizacion`) y se registra con `console.error`, pero ya no aborta el resto de la pasada. `ajustes.google.errores` (nuevo mensaje, con plural ICU) avisa en `PaginaAjustes` en ámbar (`--motivador`) si `errores > 0`, sin bloquear la UI.
- [x] **Tareas con hora de inicio y duración** (pedido explícitamente por el usuario junto con el arreglo, para poder representarlas como bloques de tiempo y no solo como fecha límite): `Tarea.duracionMinutos Int?` nuevo en `schema.prisma` (migración `20260922154243_duracion_minutos`), aceptado en `CrearTareaDto`/`ActualizarTareaDto` (`@IsInt() @Min(5) @Max(480)`). No se añadió un booleano "tiene hora" aparte: una tarea "tiene hora de inicio" cuando su `fechaLimite` no cae exactamente a medianoche UTC (la que deja un `<input type="date">` sin hora) — mismo criterio en el backend (`tieneHoraInicio()` en `google.service.ts`) y en el frontend (`tieneHoraInicio()` en `utilidades/fechas.ts`). Sin duración indicada se asume **30 minutos** (elegido por el usuario entre 30/60/obligatorio).
  - `GoogleService.construirEventoDesdeTarea` ahora construye un evento con hora (`start.dateTime`/`end.dateTime`, fin = inicio + duración) en vez de uno de todo el día cuando la tarea tiene hora de inicio — esto es lo que de verdad evita el `400 Bad Request` original: el evento que se reenvía a Google ya coincide en forma con el evento real, en vez de intentar aplanarlo a "todo el día".
  - Al importar un evento de Google con `start.dateTime`/`end.dateTime` (no solo `start.date`), se calcula y guarda su duración real en `duracionMinutos` (diferencia entre fin e inicio, mínimo 5 minutos) en vez de dejarla en blanco.
  - Frontend: `DetalleTarea` gana una sección "Fecha límite" editable (antes la fecha límite solo se podía fijar al crear la tarea, nunca editar después) con `<input type="date">` + `<input type="time">` opcional; si se pone hora aparece un campo "Duración (minutos)" (precargado a 30 o al valor guardado). Reutiliza el thunk `cambiarFechaLimiteTarea` ya existente en `tareasSlice.ts` (estaba definido pero sin usar en ningún sitio), ampliado para aceptar también `duracionMinutos`. La hora tecleada se trata como si ya fuera UTC (`combinarFechaYHora` en `utilidades/fechas.ts`): mismo tipo de simplificación deliberada que ya tenía la fecha límite sin hora (guardada siempre a medianoche UTC) — este proyecto no gestiona zonas horarias en ningún otro sitio, así que una hora con hora local real quedaría inconsistente con el resto. Consecuencia conocida: para un evento importado de Google, la hora que muestra FocusFlow es la hora UTC en crudo, no la hora local del evento original (p. ej. un evento a las 20:20 hora de España se ve como "18:20" en FocusFlow).
  - No se tocaron los formularios de captura rápida (Objetivos/Kanban/`ObjetivoTarjeta`): siguen siendo solo de fecha, sin hora, para no complicar la captura rápida — la hora/duración se añaden después desde el detalle de la tarea, mismo patrón ya usado para `tiempoEstimadoMinutos`.
- [x] **Página "Agenda" nueva** (`frontend/src/paginas/PaginaAgenda.tsx`, ruta `/agenda` protegida, enlace "Agenda" en `PaginaInicio`), pedida junto con lo anterior para poder "agrupar las tareas por horas y no por nombre" y "ver un día concreto, la semana o el mes": filtro Día/Semana/Mes + navegación Anterior/Hoy/Siguiente (`←`/`Hoy`/`→`), todo calculado en el frontend a partir de `tareasSlice` ya cargado (mismo patrón sin endpoint propio que Kanban/Eisenhower/Estadísticas/Revisión).
  - Utilidad nueva `frontend/src/utilidades/agenda.ts` (`obtenerDiasSemana`, `obtenerSemanasMes`, `tareasDelDia`, `ordenarTareasDelDia`): todo el cálculo de días/semanas/meses se hace en **UTC**, no en hora local, para que coincida con cómo se interpreta la hora de una tarea (ver más arriba) y una tarea no "salte" de día solo por la zona horaria de quien mira la Agenda.
  - Vista **Día**: lista de la fecha activa, tareas sin hora primero (como los eventos de todo el día en Google Calendar) y luego por hora ascendente. Vista **Semana**: 7 columnas (lunes a domingo), cada una con su propia lista ordenada igual; la columna de hoy se resalta. Vista **Mes**: cuadrícula de semanas completas (rellenando con días del mes anterior/siguiente), cada celda muestra hasta 3 títulos y un "+N más" si hay más; pulsar un día de la semana o del mes salta directamente a la vista Día de esa fecha.
  - Cada tarea de la Agenda reutiliza el componente `DetalleTarea` ya existente como título clicable, así que abrir/editar una tarea desde la Agenda es exactamente la misma experiencia que desde Kanban/Objetivos.
- [x] Tests: `utilidades/fechas.test.ts` ampliado con `tieneHoraInicio`/`obtenerSoloFecha`/`obtenerSoloHora`/`combinarFechaYHora`/`calcularFinBloque`; `utilidades/agenda.test.ts` nuevo (4 casos); `google.service.spec.ts` ampliado con 4 casos (evento con hora, duración por defecto de 30 min, que un evento que falla no aborta el resto de la sincronización, y que importar un evento con hora captura su duración real); `tareas.service.spec.ts` ampliado con el paso de `duracionMinutos`; `DetalleTarea.test.tsx` ampliado con el guardado de hora+duración; `PaginaAgenda.test.tsx` nuevo (4 casos: reparto en la vista de semana, orden sin-hora-primero-luego-ascendente en la vista de día, "+N más" en la vista de mes, mensaje vacío). **Lección de testing capturada en `comandos.md`**: combinar `vi.useFakeTimers()` (para fijar "hoy") con `userEvent.click(...)` en el mismo test cuelga el test indefinidamente, porque los temporizadores falseados por defecto también paran el planificador de React; la solución es `vi.useFakeTimers({ toFake: ['Date'] })`, que solo falsea `Date` y deja el resto de temporizadores reales.
- [x] Probado en Chrome de verdad con la cuenta real del usuario: repetida la sincronización tras el arreglo → terminó sin "Internal server error" y sin eventos marcados como fallidos ("0 creados, 183 actualizados, 1 eliminados, 1 importados desde Google") — el evento recurrente que antes rompía la sincronización ya no da problemas porque ahora se reimporta con su hora real. Creada una tarea de prueba con hora (10:00) y duración (45 min) desde `DetalleTarea`, comprobado que persiste al reabrir el diálogo, y comprobado que aparece correctamente en las tres vistas de `/agenda` (Día, Semana con las demás tareas reales del usuario ordenadas por hora, y Mes con el recuento "+N más"). Tarea de prueba borrada al terminar para no dejar datos de prueba mezclados con las tareas reales del usuario.

## Modo Escolar (ámbito Personal/Escolar + consentimiento parental)

Pedido explícitamente por el usuario para poder usar FocusFlow tanto en el ámbito personal/profesional como en el escolar (p. ej. un estudiante de la ESO con agenda escolar y tareas personales a la vez), con soporte para menores de edad (correo de un tutor legal + consentimiento). Plan completo en 6 fases (A-F); esta sesión cubre las fases A y B, con el resto pendiente. Decisiones acordadas con el usuario antes de empezar: el consentimiento se construye con un flujo de correo real (no una casilla simulada) pero sin tener aún un SMTP configurado, exactamente igual que se hizo en su día con las credenciales de Google Calendar; los exámenes/trabajos escolares se modelan ampliando la `Tarea` existente (no con modelos nuevos) para poder migrar más adelante a algo más separado si hiciera falta; hace falta un selector de ámbito activo (Personal/Escolar/Todos) que filtre de verdad las vistas, no solo un color, para poder concentrarse en un mundo sin ver el otro; el modo escolar se activa desde Ajustes, independiente de la edad; y la navegación se reorganiza en una barra lateral fija por secciones.

### Fase A — Modelo de datos + registro/consentimiento (backend)

- [x] `backend/prisma/schema.prisma`: nuevos enums `Ambito` (`PERSONAL`/`ESCOLAR`) y `TipoEscolar` (`EXAMEN`/`TRABAJO`/`PRESENTACION`). `Tarea` y `Objetivo` ganan `ambito Ambito @default(PERSONAL)` (default seguro: todo lo existente queda como Personal); `Tarea` gana además `tipoEscolar TipoEscolar?` (solo tiene sentido si `ambito = ESCOLAR`, sin constraint de base de datos, igual que ya pasa con `duracionMinutos`/`fechaLimite`). `Usuario` gana `fechaNacimiento DateTime?`, `correoTutor String?`, `consentimientoConfirmado Boolean @default(true)` (el default en `true` es importante: así la migración no bloquea retroactivamente a ningún usuario ya existente, que no tiene fecha de nacimiento registrada), `consentimientoReenviadoEn DateTime?` (para limitar reenvíos a uno cada 5 minutos) y `modoEscolarActivo Boolean @default(false)`. Migración `20260922170324_ambito_escolar_tareas_objetivos` (los dos conjuntos de cambios acabaron en una sola migración porque se editó el schema completo antes de migrar; funcionalmente da igual, es un único concepto — "modo escolar" — de todas formas).
- [x] `backend/src/comun/edad.util.ts` (`calcularEdad`, cálculo simple por año/mes/día sin librería) y su equivalente en el frontend (`calcularEdad` en `frontend/src/utilidades/fechas.ts`), duplicado a propósito en vez de compartido: el backend valida el DTO con `class-validator`, el frontend decide si mostrar el campo de correo del tutor antes de enviar nada — no hay un paquete común entre ambos proyectos.
- [x] `RegistrarUsuarioDto`: `fechaNacimiento` ahora obligatoria (`@IsDateString()`); `correoTutor` obligatorio solo si `calcularEdad(fechaNacimiento) < 18` (`@ValidateIf`). `CrearTareaDto`/`CrearObjetivoDto` (y sus `ActualizarXDto` vía `PartialType`) ganan `ambito?`/`tipoEscolar?` opcionales. El filtro por ámbito **no** se hace en el backend (`filtrar-tareas.dto.ts` no se toca): sigue siendo 100% client-side, como todo el resto de vistas de esta app.
- [x] `TareasService.crear`/`crearSiguienteOcurrencia` y `ObjetivosService.crear` pasan `ambito`/`tipoEscolar` igual que el resto de campos; una tarea escolar recurrente conserva su ámbito y tipo en la siguiente ocurrencia automática (antes de este cambio no había ámbito que conservar).
- [x] `AutenticacionService`: `registrar()` calcula la edad, guarda `fechaNacimiento`/`correoTutor`, fija `consentimientoConfirmado` a `false` para un menor (`true` para un adulto), y si es menor firma un JWT de confirmación (mismo patrón que el `state` de Google: `{sub, tipo:'confirmacion-consentimiento'}`, sin guardarlo en la base de datos, solo verificable) y pide a `CorreoService` que lo envíe — envuelto en un try/catch que absorbe específicamente el "correo no configurado" para que el registro nunca falle solo por eso (se loguea un aviso en el servidor y ya está; el correo se puede reenviar después). Nuevo `confirmarConsentimiento(token)` (verifica el JWT, marca `consentimientoConfirmado: true`) y `reenviarConfirmacion(usuarioId)` (límite de un reenvío cada 5 minutos vía `consentimientoReenviadoEn`; aquí, a diferencia del registro, si el correo no está configurado el error sí se propaga tal cual, porque el usuario lo ha pedido explícitamente pulsando un botón). Nuevo `actualizarPreferencias(usuarioId, modoEscolarActivo)`, construido ya en esta fase aunque su UI (el interruptor en Ajustes) es de la Fase F. `obtenerUsuarioPorId`/`generarRespuestaAutenticacion` devuelven ahora también `consentimientoConfirmado`/`modoEscolarActivo` (nunca `contrasena`/`correoTutor`/`fechaNacimiento`, igual de estricto que antes).
- [x] Nuevas rutas en `AutenticacionController`: `POST /autenticacion/confirmar-consentimiento` (**pública** a propósito: el enlace del correo apunta a una página del frontend sin sesión iniciada, que llama a este endpoint con el token recibido), `POST /autenticacion/reenviar-confirmacion` (protegida) y `PATCH /autenticacion/preferencias` (protegida).
- [x] Nuevo guard `backend/src/autenticacion/guardias/consentimiento-confirmado.guard.ts` (`GuardaConsentimientoConfirmado`): relee `consentimientoConfirmado` de la base de datos en cada petición (el JWT no lo lleva, para no tener que reemitirlo cuando un tutor confirma) y bloquea con `403 Forbidden` si es `false`. Aplicado junto a `AuthGuard('jwt')` en `TareasController`, `ObjetivosController`, `PomodoroController`, `EtiquetasController`, `SubtareasController` y las 4 rutas autenticadas de `GoogleController` (nunca en `AutenticacionController`, que debe seguir respondiendo mientras la cuenta está pendiente). Así el bloqueo real vive en la API, no solo en la ruta protegida del frontend (que todavía no se ha construido — eso es la Fase D).
- [x] Frontend (solo tipos, sin UI de bloqueo todavía — eso es la Fase D): `UsuarioSesion` gana `consentimientoConfirmado`/`modoEscolarActivo`; `Tarea` gana `ambito`/`tipoEscolar`; `Objetivo` gana `ambito`. Nuevas funciones en `servicios/autenticacion.ts` (`confirmarConsentimiento`, `reenviarConfirmacion`, `actualizarPreferencias`), sin thunks todavía.
- [x] **Ajuste no planeado originalmente pero necesario ya**: como el DTO de registro exige `fechaNacimiento` desde ya, `PaginaRegistro.tsx` tuvo que actualizarse en esta misma fase (no se pudo esperar a la Fase F) — si no, el registro se habría quedado roto de inmediato. Nuevo campo "Fecha de nacimiento" (obligatorio) y, si la edad calculada es menor de 18, aparece también "Correo de tu padre, madre o tutor legal" (obligatorio solo entonces).
- [x] Tests: `edad.util.spec.ts` (backend, 5 casos, incluido 29 de febrero en año bisiesto) y su equivalente en `utilidades/fechas.test.ts` (frontend, 2 casos); `registrar-usuario.dto.spec.ts` nuevo (primer spec de DTO del proyecto, usando `class-transformer`/`class-validator` directamente, sin precedente previo que copiar); `autenticacion.service.spec.ts` ampliado (registro de menor vs. adulto, confirmar/reenviar, throttle, y una trampa real: `vi.clearAllMocks()` no borra un `mockRejectedValue` puesto en un test anterior, solo el historial de llamadas — hubo que añadir `correoServiceFalso....mockResolvedValue(undefined)` al `beforeEach` para que un test no "contaminara" el comportamiento por defecto de los siguientes); `consentimiento-confirmado.guard.spec.ts` nuevo; `tareas.service.spec.ts`/`objetivos.service.spec.ts` ampliados con `ambito`/`tipoEscolar`; los 5 specs de controlador que ahora aplican `GuardaConsentimientoConfirmado` a nivel de clase (`tareas`/`objetivos`/`pomodoro`/`etiquetas`/`subtareas`) necesitaron un `ServicioPrisma` falso añadido a sus `TestingModule`, porque Nest resuelve las dependencias de los guards de clase al compilar el módulo de test, no solo al recibir una petición real.
- [x] Probado en Chrome de verdad: registrada una cuenta de prueba con fecha de nacimiento de hace 10 años → apareció el campo de correo del tutor, se envió (sin SMTP configurado, así que solo quedó registrado en el servidor, exactamente lo esperado) y la cuenta quedó operativa a nivel de sesión pero con `GET /objetivos` devolviendo `403` de verdad (comprobado en el panel de red) — confirma que el guard de la API funciona aunque la UI todavía no lo comunique (ver Fase D). Registrada también una cuenta de prueba adulta → sin campo de tutor, sin bloqueo, `200` normal. Ambas cuentas de prueba se registraron con correos `@example.com` a propósito, nunca con el correo real del usuario (que Chrome autocompletaba solo en el formulario).

### Fase B — Servicio de correo (nodemailer)

- [x] Nueva dependencia `nodemailer` (+ `@types/nodemailer`). Nuevo módulo `backend/src/correo/` (`ModuloCorreo`, `CorreoService.enviarCorreoConfirmacionConsentimiento(destinatario, enlace)`), importado por `ModuloAutenticacion`. Mismo patrón exacto que `GoogleService.generarUrlAutorizacion`: si `SMTP_HOST` no está en `.env`, lanza `BadRequestException('El envío de correo no está configurado en el servidor')` antes de intentar nada; si está, usa `nodemailer.createTransport(...)`.
- [x] `.env.example`/`.env`: nuevas variables `SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/`SMTP_PASS`/`SMTP_FROM`, todas vacías salvo `SMTP_PORT="587"`, con un comentario explicando que sin ellas el registro de un menor se completa igual pero el correo no llega hasta que se configuren de verdad — igual que quedaron las credenciales de Google Calendar en su momento.
- [x] Tests: `correo.service.spec.ts` nuevo (2 casos), mockeando `nodemailer` entero con `vi.mock` igual que ya se hacía con `googleapis` en `google.service.spec.ts`.
- [ ] **Pendiente para el usuario** (igual que con Google Calendar en su momento): para que el correo de confirmación llegue de verdad a un tutor, hace falta configurar un SMTP real (una cuenta de Gmail con contraseña de aplicación, o un proveedor tipo Mailtrap/Resend en modo SMTP) en `backend/.env`. Sin configurar, el registro de un menor funciona con normalidad, pero "Reenviar correo" (cuando exista esa pantalla, Fase D) dará el error de "no configurado".

### Fase C — Selector global de ámbito (Personal/Escolar/Todos)

- [x] `interfazSlice`: nuevo `ambitoActivo: 'TODOS' | 'PERSONAL' | 'ESCOLAR'` con la acción `cambiarAmbitoActivo`, guardado en `localStorage` (`focusflow.ambito`) igual que el tema, para que el ámbito elegido sobreviva a una recarga. Un valor guardado no válido vuelve a `TODOS`.
- [x] Nuevo `frontend/src/almacen/selectores.ts` con selectores memorizados (`createSelector`): `seleccionarTareasDelAmbito`, `seleccionarObjetivosDelAmbito` y `seleccionarHistorialPomodoroDelAmbito`. El filtro sigue siendo 100% client-side (el backend no cambia). Las sesiones de Pomodoro no tienen ámbito propio: solo se ocultan las asociadas a una tarea del otro ámbito, y las que no tienen tarea se mantienen siempre (si no, cambiar de ámbito rompería la racha de días seguidos).
- [x] Todas las vistas usan ya los selectores filtrados: Inicio (próximos vencimientos), Objetivos (objetivos + tareas sueltas), Kanban, Eisenhower, Agenda, Estadísticas (tareas, objetivos y sesiones de Pomodoro), Revisión y el selector de tarea de Pomodoro. `ObjetivoTarjeta` sigue mostrando todas las tareas de su objetivo (son su contexto, y además heredan su ámbito).
- [x] Lo que se crea no desaparece al crearlo: los thunks `crearTarea`/`crearObjetivo` usan el ámbito activo si no se indica otro (`ambitoParaNuevoElemento`); con "Todos" no se envía nada y el backend aplica su default (`PERSONAL`). Una tarea creada dentro de un objetivo hereda el ámbito del objetivo.
- [x] Nuevo componente `SelectorAmbito` (tres botones con `aria-pressed`, dentro de un `role="group"`). Colocado de forma **provisional** en `RutaProtegida` (encima de todas las páginas protegidas) y en `PaginaInicio` con sesión iniciada; la barra lateral de la Fase D lo absorberá. De momento se ve siempre, aunque el modo escolar no esté activado; en la Fase F se mostrará solo con el interruptor de Ajustes encendido.
- [x] `DetalleTarea`: nuevo desplegable "Ámbito" (Personal/Escolar) con el thunk `cambiarAmbitoTarea` (PATCH `{ ambito }`). Los objetivos todavía no se pueden cambiar de ámbito después de crearlos: no hay ninguna UI para editar objetivos (el `PATCH /objetivos/:id` ya existe en el backend).
- [x] i18n: claves `ambito.*` y `tarea.detalle.ambito` en los 6 idiomas.
- [x] Tests (frontend 129 → 141): `interfazSlice.test.ts` (+3: valor por defecto, guardado y recuperación del ámbito con `vi.resetModules()`), `selectores.test.ts` nuevo (5: filtrado, Pomodoro, memorización y ámbito por defecto al crear), `SelectorAmbito.test.tsx` nuevo, `PaginaKanban.test.tsx` (+2: el selector oculta las tareas personales; una tarea creada con "Escolar" activo se envía con `ambito: 'ESCOLAR'`) y `DetalleTarea.test.tsx` (+1: cambiar el ámbito hace el PATCH).
- [x] Probado contra la API real (backend + Postgres en marcha) con una cuenta de prueba adulta con correo `@example.com`: una tarea y un objetivo creados con `ambito: ESCOLAR` se guardan y se devuelven como escolares, y un PATCH de `ambito` sobre una tarea personal la pasa a escolar.
- [x] Probado en Chrome de verdad con la cuenta de prueba adulta de la Fase A (correo `@example.com`, nunca la cuenta real del usuario): en Kanban, una tarea creada en "Todo" queda como personal y una creada con "Escolar" activo queda como escolar; cada filtro muestra solo la suya y "Todo" muestra las dos. Cambiar "Hacer la compra" a Escolar desde el desplegable "Ámbito" del detalle la mueve al filtro Escolar. El ámbito elegido se mantiene al recargar la página. Estadísticas cuenta solo las tareas del ámbito activo, y un objetivo creado en Escolar ("Aprobar 4º ESO") desaparece al pasar a Personal. Las tareas y el objetivo de prueba se quedan en esa cuenta de prueba (no mezclan nada con los datos reales).

### Fase D — Barra lateral y pantalla de cuenta pendiente

- [x] Nuevo `DisenoAplicacion` (marco común de todas las páginas con sesión): barra lateral fija de 240 px en escritorio (`md:` en adelante) y, en el móvil, una cabecera con botón de menú que abre la misma barra como cajón lateral con fondo oscurecido (se cierra al elegir una sección o al pulsar fuera). `RutaProtegida` ahora solo redirige a `/login` sin sesión y, con sesión, envuelve la página en `DisenoAplicacion`. La ruta `/` usa el mismo marco con sesión iniciada, y sigue siendo la portada pública sin sesión.
- [x] Nuevo `BarraLateral`: logo (enlace a Inicio), `SelectorAmbito` (que deja de estar encima de cada página, como se preveía en la Fase C) y tres secciones con iconos de `lucide-react` (ya estaba instalado, pero es la primera vez que se usa): **Planificar** (Inicio, Objetivos, Agenda, Kanban, Eisenhower), **Concentrarse** (Pomodoro) y **Revisar** (Revisión semanal, Estadísticas). Abajo: Ajustes, tema, idioma, nombre del usuario y Cerrar sesión. Los enlaces son `NavLink` con `end`, para que "Inicio" (`/`) no quede marcado como activo en todas las rutas.
- [x] Quitado el botón "FocusFlow" de vuelta a inicio de las 8 páginas que lo repetían, y la fila de 9 botones de `PaginaInicio` (con sesión, Inicio queda como un panel con el saludo y los próximos vencimientos). Borradas de los 6 idiomas las claves `inicio.irA*`, que ya no se usan.
- [x] Nuevo `PantallaConsentimientoPendiente`: si `usuario.consentimientoConfirmado` es `false`, `DisenoAplicacion` la muestra en lugar de la página (sin barra lateral). Explica al menor que se ha enviado un correo a su tutor y ofrece tres botones: **Ya lo ha confirmado** (vuelve a pedir `GET /autenticacion/perfil` con `restaurarSesion`; si ya está confirmado, la app se desbloquea sola), **Reenviar el correo** (muestra el mensaje del servidor si falla, p. ej. "no configurado" o el límite de 5 minutos) y **Cerrar sesión**.
- [x] Nueva página pública `/confirmar-consentimiento?token=...` (`PaginaConfirmarConsentimiento`), que es adonde apunta el enlace del correo que recibe el tutor (el backend ya lo generaba así con `FRONTEND_URL`). Llama a `POST /autenticacion/confirmar-consentimiento` y muestra la confirmación o el error ("enlace caducado o inválido", o que falta el token).
- [x] La captura rápida se centra en la columna de contenido en escritorio (no en toda la ventana) y se oculta mientras el cajón del móvil está abierto, porque si no tapaba Ajustes y Cerrar sesión (detectado al probarlo en Chrome).
- [x] i18n: claves `nav.*` y `consentimiento.*` en los 6 idiomas.
- [x] Tests (frontend 141 → 154): `BarraLateral.test.tsx` nuevo (4: secciones y enlaces, enlace activo sin marcar Inicio, selector de ámbito, cerrar sesión — este último movido desde `PaginaInicio.test.tsx`), `DisenoAplicacion.test.tsx` nuevo (3: contenido normal, bloqueo de un menor sin confirmar, cajón del móvil), `PantallaConsentimientoPendiente.test.tsx` nuevo (3: reenvío correcto, error del servidor, "Ya lo ha confirmado" desbloquea) y `PaginaConfirmarConsentimiento.test.tsx` nuevo (3: con token, token caducado, sin token).
- [x] Probado en Chrome: barra lateral en escritorio (Kanban e Inicio, enlace activo resaltado), cajón en el móvil a 420 px de ancho (se abre, navega a Estadísticas y se cierra) y la página de confirmación sin token y con un token falso. Por la API, con una cuenta de menor de prueba (`menor.fased@example.com`, tutor `tutor.fased@example.com`): queda con `consentimientoConfirmado: false`, "reenviar" responde "El envío de correo no está configurado en el servidor" y `GET /tareas` da `403`.
- [ ] **Pendiente de ver en el navegador**: la pantalla de cuenta pendiente con una sesión de menor de verdad. Claude no escribe contraseñas en el navegador, así que hay que iniciar sesión a mano con esa cuenta de prueba (contraseña `PruebaMenor1234`). Tampoco se ha podido probar el enlace de confirmación con un token válido, porque ese token solo viaja en el correo y todavía no hay SMTP configurado (ver Fase B).

### Pendiente (Fases E-F, no empezadas todavía)

- [ ] **Fase E**: página "Planificador escolar" (próximos exámenes, trabajos y presentaciones).
- [ ] **Fase F**: interruptor de "Modo escolar" en Ajustes, y pulido final de i18n en los 6 idiomas para todo lo introducido en las fases A-E.

## Roadmap de ampliación (propuesto, sin empezar salvo lo de arriba)

Bloques de la propuesta original de ampliación de funcionalidades. Los bloques 1 a 4 ya están hechos (ver sus secciones "Fase 5", "Bloque 2", "Bloque 3" y "Bloque 4" más arriba), y la sincronización con Google Calendar también (ver su sección justo arriba). Solo queda, sin empezar y a propósito:

- [ ] **Colaboración** (compartir objetivos, comentarios) e **integraciones con otras herramientas de tareas** (Todoist, Trello): descartadas por ahora por ser un salto de complejidad mucho mayor (modelo de permisos/roles, APIs de terceros adicionales); no empezar sin pedirlo explícitamente.

## Decisiones ya tomadas (para no volver a preguntarlas)

- Frontend: React + TypeScript, Tailwind CSS + shadcn/ui, Redux Toolkit, react-intl.
- Backend: NestJS + TypeScript, Prisma, PostgreSQL (no MySQL).
- Autenticación propia (bcrypt + JWT), sin servicios externos.
- Monorepo con carpetas `/frontend` y `/backend` separadas.
- Gestor de paquetes: pnpm.
- Contenedores: Docker + Docker Compose para PostgreSQL en desarrollo.
- Nombres de funciones/variables/componentes en castellano en todo el código.
- Repo GitHub: `anaborrellrichart79-debug/focusflow`.
- Prisma fijado a la versión estable `7.10.0` (la etiqueta "latest" del paquete apuntaba a una release candidate `8.0.0-rc.15`, evitada a propósito por no ser estable).
- Prisma 7 requiere un "driver adapter" explícito para conectar a la base de datos: se usa `@prisma/adapter-pg` (paquete `pg`) en `ServicioPrisma`, pasando `DATABASE_URL` como `connectionString`.
- Docker Desktop instalado con winget (motor basado en WSL2, ya configurado en esta máquina).
- El endpoint `/autenticacion/perfil` consulta siempre la base de datos (no confía solo en los datos del JWT) para que el nombre de usuario esté actualizado tras iniciar sesión de nuevo o recargar la página.
- La sesión del frontend se restaura automáticamente al cargar la app: si hay un token guardado en `localStorage`, se llama a `/autenticacion/perfil`; si el token ya no es válido, se borra.
- Modelo de datos: un `Objetivo` agrupa `Tarea`s (mini-tareas), pero una `Tarea` también puede existir suelta (sin objetivo) para permitir captura rápida estilo GTD. Al borrar un `Objetivo` se borran en cascada sus tareas.
- El progreso de cada objetivo (barra + "X de Y tareas") se calcula en el frontend a partir de las tareas ya cargadas en el store (`tareasSlice`), no del valor `totalTareas`/`tareasCompletadas` que devuelve el backend al listar objetivos — así se mantiene actualizado al instante al añadir/completar tareas sin tener que recargar la lista de objetivos.
- El temporizador Pomodoro en sí (cuenta atrás, fases, ciclos) sigue siendo puramente local en `pomodoroSlice` — no hay llamadas al backend mientras corre. Solo al completarse una fase se dispara una llamada para guardar esa sesión en el historial (`SesionPomodoro`), de forma asociativa (opcional) a una tarea concreta.
- El campo `Tarea.completada` (booleano) se sustituyó por `Tarea.estado` (enum `POR_HACER` / `EN_PROCESO` / `HECHA`) para poder soportar el tablero Kanban de 3 columnas sin tener dos fuentes de verdad sobre el estado de una tarea. El checkbox de "completada" en Objetivos/Tareas ahora alterna entre `POR_HACER` y `HECHA`.
- El tablero Kanban admite arrastrar y soltar tarjetas entre columnas (drag-and-drop nativo de HTML5, sin librería externa) además de los botones ←/→, que se mantienen porque son la única forma de mover tareas con teclado/lector de pantalla.
- La priorización de Eisenhower se guarda como dos booleanos independientes (`urgente`, `importante`) en `Tarea`, no como un único enum de 4 valores, porque son dos criterios ortogonales que además se muestran como dos botones independientes en la UI.
- La captura rápida (GTD) reutiliza el mismo endpoint/acción de crear tarea suelta que ya existía en Objetivos/Kanban; lo único nuevo es que el formulario está siempre visible (fijo en la parte inferior) en vez de vivir solo dentro de una pantalla concreta.
- Para el gráfico de "Distribución de tareas" en Estadísticas se siguió el procedimiento del skill de dataviz: se sustituyeron los `--chart-1/2/3` grises por defecto de shadcn por la paleta categórica validada (azul/naranja/aguamarina) del skill, que garantiza distinción para daltonismo en las 3 primeras posiciones tanto en claro como en oscuro. No se añadió ninguna librería de gráficos: las barras son simples `div` de Tailwind.
- La página de Estadísticas no tiene endpoint propio en el backend: reutiliza las mismas listas de `/objetivos` y `/tareas` ya cargadas en Redux y calcula todo (progreso global, distribución, Pareto) en el frontend, igual que hacen Kanban y Eisenhower.
- Google Calendar es la única integración externa con una API de terceros en todo el proyecto: se usa el paquete oficial `googleapis` en vez de hablar directamente con los endpoints REST de OAuth2/Calendar, porque ahí sí compensa apoyarse en una librería mantenida (firma de peticiones, renovación de tokens) en vez de reinventarlo, a diferencia del resto de decisiones de "sin librería externa" tomadas en otras partes de la app (CSV, PDF, drag-and-drop).
